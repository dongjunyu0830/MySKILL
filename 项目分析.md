# 项目分析：基于 U-Net 的遥感图像语义分割

> 本文档由对课程首节课老师提供的文件进行分析整理而成，用于理解项目结构与技术流程。
> 具体作业目标待老师后续发布后再补充。

## 一、项目定性

这是一个**遥感图像语义分割**的课程设计项目（数字工程课程设计），核心技术是 **U-Net 深度学习网络**。

老师首节课提供的是一套**完整的参考代码 + 三个已划分好的数据集**，作为后续作业的基础框架和练手材料。

- **任务本质**：给图像的每个像素分类（判断它属于哪种地物），输入 RGB 遥感图 → 输出逐像素的类别图。
- **应用场景**：耕地/水体/建筑/林地等地物的自动识别与提取。

## 二、目录结构总览

```
home_work/
├── code/my_unet/          # 老师提供的核心代码
├── BDCI2017-spilt/        # 数据集1（5类，城市地物）
├── DFC22-split/           # 数据集2（12类）
├── WHDLD-spilt/           # 数据集3（武汉大学遥感数据集）
├── 环境配置.md            # 老师给的环境搭建说明
├── README.md              # 本地 Python 环境使用说明
├── requirements.txt       # Python 依赖清单
└── venv/                  # 本地 Python 虚拟环境
```

## 三、三个数据集

每个数据集都已划分为 `train / val / test`，均为 `image`（原图）+ `label`（标注掩码）成对结构：

| 数据集 | 类别数 | 训练量级 | 说明 |
|--------|--------|----------|------|
| **BDCI2017** | 5 类 | ~1345 张 | 图像 256×256，5 类地物 |
| **DFC22** | 12 类 | ~536 张 | 图像 512×512，IEEE 数据融合竞赛数据 |
| **WHDLD** | 7 类 | ~3458 张 | jpg 图 + png 标签，武大遥感数据集 |

- 各数据集下的 `out` / `vis` 目录是老师之前跑出来的**预测结果与可视化图**，可作参考对照。
- 标签图（label）中每个像素的取值是类别编号（0, 1, 2, ...），并非普通彩色图。

## 四、代码模块（`code/my_unet/`）

### 模型（`model/`）
- `unet.py` — 标准 U-Net（编码器-解码器 + 跳跃连接），使用 GroupNorm 归一化。
- `mobilenet_unet.py` — 以 MobileNetV2 为编码器的轻量版 U-Net（带 ImageNet 预训练权重），参数量更小、更快。

### 数据加载（`datasetconfig/`）
- `dataset.py` — 带数据增强版（随机翻转 / 旋转 / 裁剪 / 颜色抖动），区分 train / val 模式。
- `datasetmulticlass.py` — 基础版，无增强；额外处理了 WHDLD 数据集 jpg 图与 png 标签的后缀差异。

### 训练 / 评估脚本
- `train.py` — 训练 BDCI2017（5 类）。
- `train2.py` — 训练 DFC22（12 类）。
- `train_mobile1.py` — 用 MobileNet-UNet 训练 WHDLD（7 类）。
- `evaluate.py` — 在测试集上计算指标（IoU / Precision / Recall / F1）。
- `prediction.py` — 单张图片预测并生成彩色分割图。
- `utils.py` — 损失函数（SoftIoU）与评估指标的计算实现。

### 训练记录（`train_logs/`）
老师保留了实验记录，便于对比：
- 不同学习率（0.001 / 0.01 / 1e-6）、不同 dataset 配置的对比日志（`.txt` / `.log` / TensorBoard events）。
- 训练好的模型权重 `.pth`（BDCI2017、DFC22 各有 best_miou 与 best_f1 两个）。
- 从命名看，`lr_0.001` 为当前效果最好的配置。

## 五、关键技术点

1. **网络结构**：U-Net 编码器逐层下采样提取特征，解码器逐层上采样恢复分辨率，跳跃连接融合浅层细节。
2. **损失函数**：交叉熵 `CrossEntropyLoss`（当前使用，`ignore_index=255`）或 SoftIoU（备选）。
3. **评估指标**：mIoU（平均交并比）、Precision（精确率）、Recall（召回率）、F1。
4. **训练技巧**：Adam 优化器 + `ReduceLROnPlateau` 学习率调度；保存 best_miou / best_f1 模型。
5. **运行环境**：老师推荐 conda + PyTorch（GPU）；代码中大量使用 `cuda`。

## 六、需要注意的问题

- **数据路径硬编码**：所有脚本里的数据路径是写死的绝对路径，且来自不同电脑（Windows 路径），例如
  `C:\Users\abc\Downloads\...`、`D:\study\UNet\...`、`E:\zj-dataset\...`。
  实际运行前需改成本机对应路径。
- **环境依赖**：本地 `venv` 需补装 `torch`、`torchvision` 才能运行训练/预测。
- **算力**：无 GPU 时训练会非常慢，建议用带 GPU 的机器，或仅用于代码阅读与小规模调试。

## 七、整体流程（面向理解）

```
准备数据(image+label) → 构建 Dataset/DataLoader → 定义 U-Net 模型
   → 训练(前向 + 损失 + 反向 + 优化) → 验证(计算 mIoU/F1) → 保存最优模型
   → 测试集评估(evaluate.py) → 单图预测可视化(prediction.py)
```

## 八、当前阶段小结

当前阶段的目标是**理解这套「U-Net 遥感图像分割」的完整流程**：
数据如何组织 → 模型如何搭建 → 如何训练 → 如何评估 → 如何预测。

等老师给出具体作业目标（如更换数据集 / 更换模型 / 调参提升指标等）后，即可在此框架上进行修改与实验。
