# 前端面试问题准备

## 一、推荐回答策略

如果面试官问到：“你在个人项目中遇到过什么问题？是怎么解决的？”  
建议主回答选择：**SSE 流式输出在 AI 找房助手中的接入与体验优化**。

原因是这个问题更适合作为前端面试回答：

1. **前端参与度高**：SSE 的连接建立、流式数据解析、页面逐字更新、异常中断、重试、停止生成等，都能体现前端工程能力。
2. **问题足够具体**：相比泛泛地说“接入大模型”，SSE 会自然引出网络协议、状态管理、用户体验、异常处理等追问。
3. **和项目强相关**：你的项目中明确写了“使用 SSE 实现与 LLM 的实时对话”，面试官很容易问到，也比较容易讲出真实感。
4. **能带出结果**：可以说明从“用户等待完整响应”优化为“边生成边展示”，降低等待焦虑，提升 AI 问答的实时性和交互体验。

另一个方向：**LLM 同一个问题稳定输出，前端如何提供参数和策略支持**，建议作为 SSE 回答后的延伸加分点，而不是主回答。

原因是它更偏 AI 工程和策略层，前端能做的是“参数透传、提示词模板管理、版本锁定、结果缓存、结构化展示和异常兜底”，但无法单独保证大模型绝对稳定输出。如果作为主回答，面试官可能继续追问模型侧、后端侧或算法侧细节，风险会更高。

## 二、简历相关技术栈可能被问的问题

| 技术栈 / 经历 | 面试官可能提问 | 推荐回答方向 |
| --- | --- | --- |
| Vue 及其生态 | 你在项目中怎么组织组件？如何处理组件通信？ | 结合活动页通用组件、工具函数沉淀，讲 props、emit、store、hooks / composables。 |
| uni-app 多端适配 | 微信小程序和 B 站小程序有哪些差异？ | 讲滚动容器、生命周期、平台 API、样式兼容、触控事件差异。 |
| 瀑布流 | 为什么不用纯 CSS columns？为什么要 JS 计算？ | 讲内容流卡片高度不定、双列平衡、绝对定位、图片加载后重新计算。 |
| 虚拟滚动 | 虚拟滚动原理是什么？你怎么做性能优化？ | 讲只渲染可视区域附近数据、占位高度、滚动位置计算、减少 DOM 数量。 |
| scroll-view / 手势切 Tab | 遇到嵌套滚动和手势抢占怎么解决？ | 讲判断滑动方向、控制滚动容器、避免 Swiper 高度塌陷、稳定触控交互。 |
| vue-i18n | 多语言活动页如何做？ | 讲文案 key 管理、语言包拆分、默认语言、兜底文案、不同语言长度适配。 |
| 登录回流 | 端内外活动登录态怎么处理？ | 讲环境判断、参数保存、登录后回跳、公共参数透传、异常场景兜底。 |
| 全链路埋点 | 你埋点怎么设计？ | 讲曝光、点击、跳转、领取、投稿、异常状态等关键节点，服务转化分析。 |
| 百度地图 WebGL / MapVGL | 万级房源点怎么展示不卡？ | 讲点聚合、热力图、WebGL 渲染、按区域筛选、避免一次性渲染过多 DOM。 |
| ECharts | 图表数据如何联动地图筛选？ | 讲区域维度、房价区间、房源数量、筛选条件统一管理。 |
| SSE | 为什么用 SSE，不用 WebSocket？ | 讲 AI 生成是服务端单向持续推送，SSE 更轻量，天然适合文本流式输出。 |
| 大文件上传 | 分片上传怎么实现？ | 讲文件切片、MD5 秒传、并发控制、进度统计、失败重试、断点续传。 |
| LLM 接入 | 如何让 AI 返回更稳定？ | 讲温度、seed、提示词模板、模型版本、结构化输出、缓存和前端兜底。 |

## 三、个人项目问题推荐回答：SSE 流式输出

### 1. 面试官问题

你在 AI 找房项目中遇到过什么比较有挑战的问题？最后是怎么解决的？

### 2. 推荐回答

我在 AI 找房助手模块中遇到的一个问题是：一开始用户提交预算、区域、通勤、户型等需求后，需要等待大模型完整生成结果后页面才展示内容。由于模型响应时间不稳定，用户会感觉页面像“卡住了”，交互体验比较差。

后来我选择使用 **SSE** 来实现流式输出。因为这个场景主要是服务端向前端持续推送文本结果，前端不需要高频双向通信，所以相比 WebSocket，SSE 更轻量，也更符合 AI 文本生成的使用场景。

实现上，我在前端发起请求后维护一个生成状态，将服务端返回的文本片段逐步拼接到当前回答中，并实时更新页面展示。同时处理了几个边界问题：比如生成中按钮置灰、防止重复提交；用户切换问题时中断旧请求；连接异常时提示用户重试；生成结束后再把完整回答和推荐条件同步到会话记录中。

这个优化之后，用户不需要等完整结果返回才能看到内容，而是可以边生成边阅读，AI 助手的实时感更强，也降低了等待过程中的不确定感。对我来说，这个问题也让我更理解前端在 AI 应用中不只是展示结果，还需要处理流式数据、状态同步和异常体验。

### 3. 可以继续补充的技术点

如果面试官追问细节，可以补充：

- SSE 是基于 HTTP 的单向推送，适合通知、日志、AI 文本生成等服务端持续输出场景。
- 和 WebSocket 相比，SSE 更简单，浏览器原生支持 EventSource，但如果需要自定义请求头或 POST 请求，通常会用 fetch + ReadableStream 读取流。
- 前端需要处理消息拼接、loading 状态、停止生成、重复提交、异常重连、页面销毁时取消请求等问题。
- 如果返回内容包含 Markdown，还要注意边生成边渲染时的格式不完整问题，可以先按纯文本展示，结束后再做完整 Markdown 渲染。

## 四、备选加分回答：LLM 稳定输出

### 1. 面试官问题

同一个找房问题，多次询问大模型可能返回不同结果。你们怎么保证输出稳定？

### 2. 推荐回答

这个问题我理解不能只靠前端完全解决，因为大模型本身存在一定随机性，稳定性通常需要前后端和模型参数共同控制。但前端可以做一些策略支持，尽量减少同一个问题返回差异过大的情况。

首先，在请求参数上，前端可以把业务侧可控参数透传给后端，比如 temperature、top_p、seed、模型版本、提示词模板版本等。对于找房推荐这种偏决策辅助的场景，我会倾向于使用较低的 temperature，让回答更稳定、更少发散。

其次，在提示词上，前端不要直接把用户输入原样丢给模型，而是把用户的预算、区域、通勤、户型等条件结构化，再套入固定的提示词模板中。这样可以减少提示词表达差异带来的输出波动。

再次，对于推荐结果，可以要求后端或模型按固定结构返回，例如推荐理由、匹配条件、风险提示、房源排序依据等字段。前端再按固定结构渲染，避免模型自由发挥导致页面展示不可控。

最后，对于相同用户、相同筛选条件、相同模板版本的问题，可以做结果缓存或历史会话复用。这样既能提升响应速度，也能让用户在相同条件下看到更一致的结果。

所以我的理解是：前端不能保证大模型绝对稳定，但可以通过参数传递、模板版本锁定、结构化输入输出、缓存和兜底展示，帮助整个 AI 功能变得更可控。

### 3. 适合作为 SSE 回答后的补充

如果你先回答了 SSE，最后可以自然补一句：

“后面我也考虑过 AI 输出稳定性的问题，比如同一个找房条件多次提问时，模型可能给出不同排序。这个问题前端不能单独解决，但可以通过低 temperature、seed、提示词模板版本、结构化输入输出和缓存策略来配合后端提升稳定性。”

## 五、实习经历高频追问准备

### 1. 喵影小程序首页重构

可能问题：

- 瀑布流为什么会错位？
- 虚拟滚动怎么计算可视区域？
- 图片高度不确定时怎么处理？
- 微信和 B 站小程序兼容差异有哪些？
- scroll-view 嵌套滚动为什么会有问题？

回答重点：

围绕“内容资源流页面性能优化”来讲，不要只说写了一个瀑布流。重点说清楚：卡片高度不定导致布局错位，长列表 DOM 过多导致卡顿，多端滚动行为不一致导致交互不稳定。你的解决方案是瀑布流高度计算、虚拟滚动、多端滚动逻辑统一和手势冲突处理，最终带来 60% 以上的渲染性能提升。

### 2. Filmora App AIGC 海外增长活动

可能问题：

- 活动页登录态怎么处理？
- 端内和端外跳转有什么区别？
- 多语言适配有哪些坑？
- 埋点怎么设计？
- 如何判断活动开发带来了提升？

回答重点：

围绕“增长活动链路”来讲，不要只说做了页面。重点说清楚：活动不是单页展示，而是包含分享引流、签到激励、一键创作、投稿领奖等完整链路。你处理了环境判断、登录回流、公共参数、库存校验、重复领取、奖池耗尽和埋点，最终降低端内外跳转流失率 20% 以上，并通过组件和工具沉淀缩短后续开发周期 35%。

## 六、最终建议

个人项目问题的主回答建议选 **SSE 流式输出**。  
它更前端、更具体、更容易讲出真实实现和结果。

**LLM 稳定输出**建议作为延伸回答或追问回答。  
它能体现你对 AI 产品工程化的理解，但不要把它讲成“前端能完全保证模型稳定”，而要讲成“前端通过参数、模板、版本、缓存和展示策略配合后端提高稳定性”。

## 七、实习项目问题推荐回答

如果面试官问到：“你在实习经历 / 实习项目中遇到过什么问题？是怎么解决的？”

建议优先回答：**喵影小程序首页重构中的瀑布流错位和长列表卡顿问题**。

这个回答更适合技术面，因为它能体现前端基础、性能优化、多端兼容和复杂交互处理能力，而且简历中有明确结果：**长列表渲染性能提升 60%+**。

### 1. 主回答：喵影小程序首页重构

面试官如果问：

“你在实习项目中遇到过什么比较有挑战的问题？最后是怎么解决的？”

可以这样回答：

我在喵影小程序首页重构中遇到的一个比较典型的问题是：首页是内容资源流场景，卡片高度不固定，而且需要同时支持微信和 B 站多端小程序。一开始如果直接用普通双列布局，图片加载完成后高度变化容易导致卡片错位、重叠；同时随着用户不断下滑，页面中渲染的卡片越来越多，深滚动时会出现明显卡顿。

我的解决思路主要分成两部分。

第一部分是解决瀑布流错位问题。我没有直接使用纯 CSS columns，而是采用 **绝对定位 + JS 高度计算** 的方式。每次渲染卡片时，根据左右两列当前累计高度，把新卡片放到较短的一列，并记录它的 top、left 和高度。对于图片高度不确定的问题，会在图片加载完成后重新计算卡片高度，避免因为异步加载导致布局错乱。

第二部分是解决长列表性能问题。我引入了 **虚拟滚动**，只渲染可视区域附近的数据，通过占位高度维持整体滚动条高度，再根据 scrollTop 计算当前应该展示的列表范围。这样可以减少页面上的真实节点数量，避免一次性渲染大量卡片导致页面卡顿。

另外，因为项目是 uni-app 多端小程序，我还统一处理了微信和 B 站小程序在 scroll-view、触控事件和高度计算上的差异，并处理了手势滑动切 Tab 时的嵌套滚动和高度塌陷问题。

最终结果是：首页深滚动卡顿明显减少，长列表渲染性能提升了 60% 以上，瀑布流错位和重叠问题也得到了稳定解决。

### 2. 这个回答可能被追问的问题

| 追问问题 | 回答方向 |
| --- | --- |
| 为什么不用 CSS columns？ | CSS columns 对卡片顺序和精确位置控制较弱，不方便结合虚拟滚动和多端高度计算。 |
| 瀑布流高度怎么计算？ | 维护左右两列累计高度，新卡片插入较短列，记录 top、left，并在图片加载后更新高度。 |
| 虚拟滚动怎么实现？ | 根据 scrollTop、容器高度、预渲染数量计算展示区间，通过上下占位保持滚动高度。 |
| 图片加载后高度变化怎么办？ | 图片加载完成后重新计算卡片高度和后续卡片位置，避免错位。 |
| 多端兼容主要差异是什么？ | scroll-view 行为、触控事件、生命周期和部分样式表现存在差异，需要统一封装。 |
| 手势切 Tab 为什么会冲突？ | 横向滑动和纵向滚动容易抢占事件，需要判断滑动方向并控制滚动容器行为。 |

### 3. 备选回答：Filmora AIGC 增长活动

如果面试官更关注业务理解、协作经验或增长活动，可以回答：**Filmora App AIGC 增长活动中的端内外登录回流和复杂活动链路问题**。

可以这样回答：

在 Filmora App 海外 AIGC 增长活动中，我遇到的主要问题是活动链路比较长，而且涉及端内、端外多个环境。用户可能从分享链接进入，也可能从 App 内进入；进入后还涉及登录态判断、签到领奖、一键创作、投稿领奖、库存校验、重复领取、奖池耗尽等分支。如果这些状态处理不好，很容易出现跳转丢参数、登录后无法回到原页面、用户领奖失败但前端没有明确提示等问题。

我的处理方式是先把整个活动链路拆成几个关键节点：入口来源、环境判断、登录状态、任务状态、奖品状态和创作跳转。然后封装了环境判断、登录回流、公共参数获取等通用逻辑，保证端内外进入都能拿到一致的活动参数。对于库存不足、重复领取、奖池耗尽等异常分支，也统一做了状态判断和提示。

同时，我补充了全链路埋点，比如活动曝光、按钮点击、登录跳转、创作转化、领奖成功和失败原因等，方便后续分析用户在哪个环节流失。

最终这个活动降低了端内外跳转流失率 20% 以上，并且沉淀了一些通用组件和工具方法，让后续同类活动开发周期缩短了大约 35%。

### 4. 实习项目回答选择建议

技术面优先回答：**喵影小程序首页重构：瀑布流 + 虚拟滚动 + 多端兼容**。  
这个方向更容易体现你的前端技术深度，也更容易被追问到具体实现。

业务面或 HR 面可以回答：**Filmora AIGC 增长活动：端内外链路 + 登录回流 + 埋点 + 业务分支**。  
这个方向更适合体现你对业务链路、用户转化和工程沉淀的理解。

最稳妥的表达方式是：先讲喵影首页重构作为主案例，如果面试官继续追问其他实习内容，再补充 Filmora 增长活动。这样能同时体现技术深度和业务理解。

## 八、实习中使用 AI 进行项目开发的回答方向

如果面试官问到：“你在实习过程中有没有使用 AI 辅助开发？具体怎么用的？”

不要回答成“我让 AI 帮我写代码”。更好的回答是：**我把 AI 当作工程协作工具，主要用于业务流程分析、方案文档沉淀、上下文记忆维护、代码实现辅助和风险复查**。

可以参考 `D:\fx_operation_frontend\fx_operation_frontend` 这个项目来回答。这个项目本身是一个 `Vue 3 + Vite` 前端项目，同时承载了多套历史 H5 活动页，包含端内外环境判断、登录回流、JSBridge、活动接口、埋点、多语言和奖品链路等复杂业务。AI 在这种项目中最有价值的地方，不是直接生成某一段代码，而是帮助降低复杂业务的理解成本和维护成本。

### 1. 推荐主回答

可以这样回答：

我在实习项目中使用 AI 的方式，不是简单让它直接生成页面代码，而是把它放进一个比较固定的工程流程里。因为活动页项目往往有很多历史代码、端内外差异、登录回流、埋点和活动配置，如果直接让 AI 改代码，很容易漏掉业务分支。

所以我通常会先让 AI 做业务流程分析，先从用户进入页面后的真实路径出发，梳理端内 / 端外、登录 / 未登录、会员态、活动状态、任务状态等分支，明确每个按钮点击后是停留当前页、弹窗、toast、跳 H5，还是交给 App deeplink 或原生页面处理。

在分析清楚之后，我会建立或更新 Plan 文档，把本次需求的范围边界、固定实现方案、接口配置、跳转规则、状态字段、初始化流程、埋点方案、开发顺序和验收标准写清楚。后续实现时，不是让 AI 临时发挥，而是要求它严格根据 Plan 执行；如果需求变化，也先更新 Plan，再改代码。

同时，我会维护项目 memory 系统，把临时任务记录、稳定项目规则和长期协作习惯分层保存。比如会话层记录当前任务目标、涉及文件、已确认结论和待确认问题；项目层记录稳定的架构、接口规则、调试经验和业务决策；长期层记录可复用的协作模式。这样后续再处理同类活动页时，AI 不需要每次从零理解项目，也能减少忘记历史规则、误用旧代码或重复踩坑的问题。

最后在实现阶段，我会让 AI 辅助完成代码定位、局部修改、语法检查和风险复查，但关键业务规则还是由我确认。比如涉及登录回流、端内外跳转、埋点口径、奖品状态、库存状态这类逻辑，我会重点 review diff，并通过本地构建、语法检查或关键路径自测来验证。

整体来说，我使用 AI 的核心不是“替我写代码”，而是把它作为一个能持续维护上下文、拆解业务流程、沉淀方案并辅助实现的工程协作工具，从而提升复杂活动页开发的稳定性和效率。

### 2. 回答方向一：构建项目 memory 系统

这个方向适合体现你对 AI 协作的工程化理解。

可以这样讲：

在活动页项目中，我会先构建项目 memory 系统，避免 AI 每次对话都从零开始理解项目。memory 通常分为三层：

- **session 会话层**：记录当前任务目标、涉及文件、执行计划、已确认结论、待确认问题和最终结果。
- **project 项目层**：记录稳定的项目事实，比如项目架构、启动命令、目录职责、接口规则、JSBridge 规则、埋点规则、已确认业务决策和常见调试方式。
- **long_term 长期层**：记录跨任务可复用的协作模式，比如先读项目记忆、先写 Plan、改完后把稳定结论回填到项目记忆。

结合 `D:\fx_operation_frontend\fx_operation_frontend` 项目可以说：

这个项目里通过 `AGENTS.md` 约定了 AI 的工作流：实现前先读 `memory/project/project.md`、`architecture.md`、`decisions.md`；任务过程中在 `memory/session/active/` 维护会话记录；任务完成后把稳定规则晋升到 `memory/project/decisions.md` 或 `debugging.md`。比如毕业季活动中，端内外承接规则、公共参数获取方式、插件 ID、埋点口径、iOS 购买后轮询规则等，都可以沉淀到项目层，后续处理奖品页、投稿页或同类活动时就能复用。

这个方向的结果可以这样表达：

这样做的好处是减少 AI 对历史业务规则的遗忘，避免每次都重新解释项目背景，也能让复杂项目中的接口规则、跳转规则和埋点口径保持一致。

### 3. 回答方向二：先做业务流程分析，再写 Plan 文档

这个方向最适合和你的实习经历结合，因为增长活动页本身业务链路复杂。

可以这样讲：

我会先用 AI 辅助做前端业务流程分析，而不是一上来就分析函数或改代码。分析时主要围绕用户路径展开：用户进入页面后先发生什么，页面如何判断端内外、登录态、会员态和活动状态，用户看到什么内容，点击按钮后是弹窗、toast、跳转 H5，还是交给 App deeplink 承接。

比如毕业季活动首页中，AI 可以帮助分析端外用户进入页面时是否自动弹出 `Open Filmora App` 弹窗。通过分析后可以确认：`openAppModalVisible` 初始值是 `false`，端外初始化只展示底部 Open 条，不会自动弹窗；只有用户点击受限业务按钮后，才会通过 `beforeCheck()` 打开引导弹窗。这样的分析能避免误把“端外引导”做成进入页面就强制弹窗。

业务流程分析完成后，我会建立 Plan 文档。Plan 里会明确：

- 当前必须交付什么，不做什么。
- 页面使用什么技术方案。
- 端内外、登录态、会员态、活动状态分别怎么处理。
- API、请求头、活动 ID、插件 ID、deeplink 和外链如何集中配置。
- 初始化流程、刷新流程、埋点规则和验收标准是什么。
- 哪些信息已经确认，哪些还需要产品或运营确认。

在 `D:\fx_operation_frontend\fx_operation_frontend` 项目中，毕业季活动就维护了首页 Plan 和奖品页 Plan。首页 Plan 负责主活动页的模块顺序、登录赠送、购买、投稿、资源、规则弹窗、端外引导和埋点；奖品页 Plan 只负责奖品列表、登录态、端外态、邮箱收集和动作分发。这样可以避免一个文档里混杂太多职责，也能降低后续实现时的上下文成本。

这个方向的结果可以这样表达：

通过“先分析业务流程，再建立 Plan，再根据 Plan 实施”的方式，AI 输出会更稳定，不容易遗漏端内外、登录回流、埋点和异常分支。即使需求中途变化，也能先改 Plan，再改代码，避免代码和方案脱节。

### 4. 回答方向三：用 AI 辅助处理复杂链路和边界场景

这个方向适合举实习项目中的实际问题。

可以讲这些例子：

| 场景 | AI 可以辅助什么 | 体现的能力 |
| --- | --- | --- |
| 端内外活动页 | 梳理端内 / 端外用户看到什么、点击后走弹窗还是 deeplink | 业务流程分析能力 |
| 登录回流 | 梳理未登录跳登录、登录后回到原页面、重新获取公共参数和刷新状态 | 状态同步能力 |
| 奖品页 | 分析已登录、未登录、端外、空列表、奖品动作等显示模式 | 分支建模能力 |
| iOS 购买后刷新 | 分析为什么不能只依赖 `visibilitychange`，规划 10 分钟公共参数轮询 | 平台差异处理能力 |
| 埋点 | 对齐埋点说明文档，检查曝光、点击、离开、toast 是否漏报 | 数据链路意识 |
| 多语言 | 检查语言 key、兜底语言和不同语言文案长度 | 国际化细节 |
| 历史页面复用 | 区分哪些旧页面逻辑可直接复用，哪些只能作为行为参考 | 迁移和复用判断能力 |

可以展开一个具体例子：

比如在处理 iOS 购买后状态刷新时，AI 先帮我分析现有页面的公共参数获取逻辑、购买点击逻辑和 `visibilitychange` 刷新链路。分析后发现 iOS 原生支付弹层不一定触发 H5 的 `visibilitychange`，所以不能只依赖页面重新可见来刷新购买状态。后续方案是复用页面已有的公共参数轮询控制器，点击购买后开启购买态轮询，只比较 `vip_type` 是否变化；如果会员态变化就刷新购买卡文案，如果超过 10 分钟就停止。这个方案也避免了多个 timer 同时存在导致状态互相覆盖。

### 5. 回答方向四：用 AI 做实现后的复查和沉淀

这个方向适合在回答最后补充，体现你不会盲信 AI。

可以这样讲：

代码实现完成后，我不会直接认为 AI 生成的代码就是正确的，而是会继续让 AI 做一次偏“挑刺”的复查。重点检查：

- 是否遗漏端外、未登录、活动结束、库存耗尽、重复领取等分支。
- 是否把历史页面里的活动 ID、SKU、资源 ID 等常量错误复用到新活动。
- 是否漏掉埋点、UTM 参数、透明导航参数或登录回流参数。
- 是否出现多个轮询 timer、重复请求、状态互相覆盖等问题。
- 是否破坏多语言 key、页面结构或原有历史 H5 页面。

复查后，如果发现某个结论未来还会复用，就把它沉淀到项目 memory 中；如果只是本次任务的临时判断，就保留在 session 记录里，不随便晋升到项目层。这样既能利用 AI 的效率，又能控制 AI 误改和幻觉带来的风险。

### 6. 面试中可以使用的精简版回答

如果面试时间比较短，可以这样回答：

我在实习中使用 AI，更多是把它当作工程协作工具，而不是简单的代码生成器。因为我参与的活动页项目涉及端内外环境、登录回流、JSBridge、埋点、多语言和奖品链路，业务分支比较多，所以我会先让 AI 辅助分析业务流程，明确不同状态下用户看到什么、点击后发生什么。

然后我会建立 Plan 文档，把需求范围、接口配置、跳转规则、状态字段、初始化流程、埋点和验收标准固定下来。后续实现时，要求 AI 按 Plan 执行；如果需求变化，就先更新 Plan，再改代码。

同时我会维护项目 memory，把当前任务记录、稳定项目规则和长期协作习惯分层保存。这样 AI 后续处理同类需求时，可以复用之前确认过的业务规则，减少重复沟通，也降低漏掉端内外、登录态、埋点等分支的概率。

所以我使用 AI 的主要价值是提升复杂业务理解、方案沉淀和实现校验的效率，而不是完全依赖它自动写代码。关键业务规则和最终代码变更，我还是会自己确认和 review。

### 7. 面试回答注意事项

- 不要说“我主要靠 AI 写代码”，容易让面试官怀疑你的独立开发能力。
- 要强调你负责业务判断、方案确认、代码 review 和最终验证。
- 要讲具体流程：业务分析 -> Plan 文档 -> 根据 Plan 实施 -> 验证 -> memory 沉淀。
- 要讲 AI 带来的结果：减少重复理解成本、降低遗漏分支概率、提升同类活动开发效率、让复杂规则更可维护。
- 如果面试官追问“AI 生成错了怎么办”，回答重点是：小步修改、人工 review、运行检查、关键路径自测、稳定结论才沉淀到 memory。
